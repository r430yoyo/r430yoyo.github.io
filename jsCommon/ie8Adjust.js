$(document).ready(initAdjust);

function initAdjust() {
	$("#centerMenu").css({backgroundSize: "cover"});
	$(".imgContainer.active").css({backgroundSize: "cover"});
	//$(".imgContainer.next").css({backgroundSize: "cover"});
}